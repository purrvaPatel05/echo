fastapi>=0.115
uvicorn[standard]>=0.30
python-socketio>=5.11
pydantic>=2.7
# Not used by the mock yet -- wired in when we swap the in-memory store out:
# sqlalchemy>=2.0
# psycopg[binary]>=3.2
# celery[redis]>=5.4
# anthropic>=0.40
